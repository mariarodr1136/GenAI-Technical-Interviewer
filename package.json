{
  "name": "genai-technical-interviewer",
  "version": "0.1.0",
  "private": true,
  "description": "A free-tier GenAI technical interviewer using React, Express, Groq STT, Groq LLM, and browser speech synthesis.",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c cyan,magenta \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "dev:client": "npm run dev --workspace client",
    "dev:server": "npm run dev --workspace server",
    "start": "npm run start --workspace server"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
